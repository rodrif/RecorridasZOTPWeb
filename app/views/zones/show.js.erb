$('#new_zone_modal').modal_success();
$('#person_area_id').val(<%= @zone.area.id %>);
$('#person_zone_id').html("<%= escape_javascript select 'person', 'zone_id', Zone.where(area_id: @zone.area.id).order(:nombre).collect {|z| [z.nombre, z.id]}, :include_blank => false  %>");
$('#person_zone_id').val(<%= @zone.id %>);
$('#person_zone_id').change();


