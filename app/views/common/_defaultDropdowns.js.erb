<% loadDefaultDropdowns %>
<% js = escape_javascript(render(partial: 'people/modals')) %>
$("#modalsDiv").html("<%= js %>");
$('body').removeClass('modal-open');
$('.modal-backdrop'). remove();

$('#zone_area_id').val($('#person_area_id').val());
$('#zone_area_id').change();

loadMapaScript('cargarUbicacionPersona');