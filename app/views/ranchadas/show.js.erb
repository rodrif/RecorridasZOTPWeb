$('#new_ranchada_modal').modal_success();
$('#person_area_id').val(<%= @ranchada.zone.area.id %>);
$('#person_zone_id').html("<%= escape_javascript options_from_collection_for_select Zone.where(area_id: @ranchada.zone.area.id).order(:nombre), 'id', 'nombre' %>");
$('#person_zone_id').val(<%= @ranchada.zone.id %>);
$('#person_ranchada_id').html("<%= escape_javascript options_from_collection_for_select Ranchada.where(zone_id: @ranchada.zone.id).order(:nombre), 'id', 'nombre' %>");
$('#person_ranchada_id').val(<%= @ranchada.id %>);
$('#person_ranchada_id').change();