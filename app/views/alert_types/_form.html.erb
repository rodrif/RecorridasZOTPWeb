<%= form_for(@alert_type) do |f| %>
  <% if @alert_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alert_type.errors.count, "error") %> prohibited this alert_type from being saved:</h2>

      <ul>
      <% @alert_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
