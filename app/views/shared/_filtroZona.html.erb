<% extraCss ||= "col-md-3" %>

<div class="row-fluid">
  <div class="col-xs-12 <%= extraCss %> margen-vert">
    <%= f.select(
      :with_zone_id,
      @filterrific.select_options[:with_zone_id],
      { include_blank: 'Zona' },
      { class: "form-control" }
    ) %>
  </div>
</div>
