<div class="row">
  <div class="col s4">
    <%= f.select(
      :with_zone_id,
      @filterrific.select_options[:with_zone_id],
      { include_blank: 'Zona' },
      { class: "form-control" }
    ) %>
  </div>
</div>
