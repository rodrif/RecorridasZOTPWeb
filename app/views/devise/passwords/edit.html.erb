<h2>Cambiar mi contraseña</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put, :class => 'form-horizontal container-fluid'}) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <%= f.hidden_field :reset_password_token %>

  <div class="form-group">
    <%= f.label :password, "Nueva contraseña", class: "col-md-1 control-label", for: "resource_password" %><br />
    <div class="col-md-10">
      <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :password_confirmation, "Confirmar nueva contraseña" , class: "col-md-1 control-label", for: "resource_password_confirmation" %>
    <div class="col-md-10">
      <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control" %>
    </div>
  </div>

  <div class="text-center">
    <%= f.submit 'Cambiar mi contraseña', class: "btn btn-primary" %>
  </div>

<% end %>

<%= render "devise/shared/links" %>
